function toggleMenu(){
    var menuToggle = documen.querySelector('.toggle');
    var menu = documen.querySelector('.menu');
    menuToggle.classList.toggle('active')
    menu.classList.toggle('active')
}
